# Streams Module

Owns active stream state.

Responsibilities:

- Track current reservations.
- Expire stale reservations.
- Surface usage to the dashboard.
- Provide broker-safe release/update operations.
