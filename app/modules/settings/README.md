# Settings Module

Owns UI-managed configuration.

Responsibilities:

- Store application settings.
- Track host paths and Docker container paths separately.
- Provide a secrets boundary for credentials and API keys.
- Avoid manual YAML editing for normal operation.
