# Synthetic test media

No media binaries are committed here. The catalog fixture uses the reserved
`media.invalid` domain and cannot contact a real provider.

A later, separately approved playback stage may generate short color-and-tone clips
locally and place them under `media/generated/`. That directory is ignored by Git.
The generated clips must contain no copyrighted source material and must be served
only inside the isolated Mac mini test environment.
